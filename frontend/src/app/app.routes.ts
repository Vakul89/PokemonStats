import { Routes } from '@angular/router';
import { PokemonStatsComponent } from '../pokemon/pokemon-stats/pokemon-stats.component';

export const routes: Routes = [
    { path: '', component: PokemonStatsComponent },
];
